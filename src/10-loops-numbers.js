// This function calculates the factorial of a number using a for loop
// Instructions: Return the factorial (n!) of the input number. You must use a for loop.
// Input: number = 5
// Output: result = 120 (5 * 4 * 3 * 2 * 1)
function calculateFactorial() {
    
}

// This function finds the sum of even numbers in a range using a while loop
// Instructions: Return the sum of all even numbers between min and max (inclusive). You must use a while loop.
// Input: min = 1, max = 10
// Output: result = 30 (2 + 4 + 6 + 8 + 10)
function sumEvenNumbers() {
    
}

// This function creates an array of multiples using a for loop
// Instructions: Return an array of the first n multiples of the given number. You must use a for loop.
// Input: number = 3, count = 4
// Output: result = [3, 6, 9, 12]
function getMultiples() {
    
}

// This function finds the first number divisible by both a and b using a while loop
// Instructions: Return the smallest number greater than start that is divisible by both a and b. You must use a while loop.
// Input: a = 3, b = 4, start = 1
// Output: result = 12
function findCommonMultiple() {
    
}

// This function generates the nth number in the Fibonacci sequence using a for loop
// The Fibonacci sequence is where each number is the sum of the previous two numbers: 1, 1, 2, 3, 5, 8, 13, 21, ...
// For example: To get the 6th number (8), we add the 4th number (3) and 5th number (5)
// Instructions: Return the nth number in the Fibonacci sequence. You must use a for loop.
// Input: n = 6
// Output: result = 8 (The sequence goes: 1, 1, 2, 3, 5, 8)
function getFibonacciNumber() {
    
}




module.exports = {
    calculateFactorial,
    sumEvenNumbers,
    getMultiples,
    findCommonMultiple,
    getFibonacciNumber
};