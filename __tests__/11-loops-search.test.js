const {
    iterativeMap,
    iterativeFilter,
    iterativeReduce,
    findPairs
} = require('../src/11-loops.search');

describe('Loops and Search', () => {
    test('iterativeMap replaces elements with their indices', () => {
        expect(iterativeMap()).toEqual();
        expect(iterativeMap()).toEqual();
        expect(iterativeMap()).toEqual();
        expect(iterativeMap()).toEqual();
    });

    test('iterativeFilter returns elements greater than target', () => {
        expect(iterativeFilter()).toEqual();
        expect(iterativeFilter()).toEqual();
        expect(iterativeFilter()).toEqual();
        expect(iterativeFilter()).toEqual();
    });

    test('iterativeReduce sums elements greater than target', () => {
        expect(iterativeReduce()).toBe();
        expect(iterativeReduce()).toBe();
        expect(iterativeReduce()).toBe();
        expect(iterativeReduce()).toBe();
    });

    test('findPairs finds adjacent pairs that sum to target', () => {
        expect(findPairs()).toEqual();
        expect(findPairs()).toEqual();
        expect(findPairs()).toEqual();
        expect(findPairs()).toEqual();
    });
});
